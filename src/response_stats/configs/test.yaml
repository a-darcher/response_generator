    n_samples: 100
    response_type: "response"

    # trial params
    trial_range: [5,6] # 10 

    # baseline firing rate params
    threshold: 1
    scale: 3

    # beta params
    beta_a_range: [1, 1.5]
    beta_multiplier_range: [1.5, 2]

    # response params
    duration_range: [0.3, 0.7] # 0.4
    
    latency_range: [] # 0.35
    
    baseline_T: 2
    stimulus_T: 2
    dt: 0.001
    induce_refractory_period: True

    gain_response_fr_threshold: 10
    gain_response_fr_scale: 3

    # supplementary trials params
    generate_supplementary_trials: True
    supplementary_threshold: 50
    supplementary_default_count: 200
    supplementary_default_factor: 5

    seed: 73
    save_dir: "/media/al/bitesized/datasets" #"/media/al/darch/response_stats/datasets"
    save_language: "python"